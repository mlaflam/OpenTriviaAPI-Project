import { createTheme } from '@mui/material/styles';

const Theme = createTheme({
  palette: {
    primary: {
      main: '#d4d4d4', // Replace with your custom primary color
    }
  },
});

export default Theme;
